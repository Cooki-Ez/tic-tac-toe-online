export function ResetButton({ onClick }) {
  return <button className="mt-2 w-16 bg-white border border-gray-500 rounded-md bg-transparent"
                 key="-1" onClick={onClick}>Reset</button>;
}
